spring:
  application:
    name: travel-insurance-api
server:
  port: 9090
app:
  security:
    username: admin
    password: change-me
  cors:
    allowed-origins:
      - http://localhost:5173
management:
  endpoints:
    web:
      exposure:
        include: health,info
